1.html全称hypertext markup language
2.超文本是标签超级大的a标签  
3.!DOCTYPE 是一个指令，一个声明  
4.DTD引用 
5.> (子代)：创建嵌套元素，例如 div>p

+ (兄弟)：创建并列元素，例如 div+p

* (乘法)：重复创建元素，例如 li*5

# (ID)：添加 ID 属性，例如 div#header

. (类)：添加 class 属性，例如 div.container

{} (文本)：在元素内添加文本，例如 h1{Title}